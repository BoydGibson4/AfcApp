<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:enableOnBackInvokedCallback="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"

        android:label="@string/app_name"
        android:roundIcon="@drawable/aberdeen_f_c__logo_2014_svg"
        android:supportsRtl="true"
        android:theme="@style/Theme.Afc"
        tools:targetApi="31">

        <!-- Launcher Activity - LogIn -->
        <activity
            android:name="application.afc.logindatabase.LogIn"
            android:theme="@style/Theme.Design.Light.NoActionBar"
            android:exported="true"
            android:screenOrientation="portrait">>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <!-- Sign Up Activity -->
        <activity
            android:name="application.afc.logindatabase.SignUp"
            android:exported="true"
            android:theme="@style/Theme.Design.Light.NoActionBar"
            android:screenOrientation="portrait">

            <!-- Keep other configurations for SignUp -->
        </activity>


        <!-- Home Activity -->
        <activity
            android:name=".HomeActivity"
            android:theme="@style/Theme.Design.Light.NoActionBar"
            android:exported="true">
        </activity>
    </application>
</manifest>
